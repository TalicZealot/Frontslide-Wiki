@{ 
    var currentAction = ViewContext.RouteData.Values["action"];
}

<div class="editor-panel">
    <ul>
        @if (this.Context.User.Identity.IsAuthenticated)
        {
            <li>@Html.ActionLink("New Page", "NewPage", "Submissions")</li>
            if ((string)currentAction == "Page")
            {
                <li>@Html.ActionLink("Edit Page", "Edit", "Edit", new { title = ViewContext.RouteData.Values["title"] }, null)</li>
            }
            if ((string)currentAction == "Index")
            {
                <li>@Html.ActionLink("Edit Page", "Edit", "Edit", new { title = "main-page" }, null)</li>
            }
            if (this.User.IsInRole("Editor") || this.User.IsInRole("Admin"))
            {
                if ((string)currentAction == "Page")
                {
                    <li>@Html.ActionLink("Manage Edits", "Edits", "Edit", new { title = ViewContext.RouteData.Values["title"] }, null)</li>
                }
                if ((string)currentAction == "Index")
                {
                    <li>@Html.ActionLink("Manage Edits", "Edits", "Edit", new { title = "main-page" }, null)</li>
                }
                <li>@Html.ActionLink("Manage Submissions", "Submissions", "Submissions")</li>
            }
        }
    </ul>
</div>